class Message < ActiveRecord::Base

	# def time_since_signing(entry)
	# 	@current_time = Time.now.strftime("%s").to_i
	# 	@time_difference = (@current_time - entry/86400)

	# 	return @time_difference
	# end

end
